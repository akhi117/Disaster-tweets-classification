# CS3012E Artificial Intelligence — Course Project
# Imbalanced Text Classification Study
# requirements.txt — Python 3.9+ compatible
#
# Install with: pip install -r requirements.txt
# For virtual environment: python -m venv venv && source venv/bin/activate
# ─────────────────────────────────────────────────────────────────────────────

# ── Core Data Science & Machine Learning ────────────────────────────────────
# Keep NumPy below 2.0 for older Python versions where TensorFlow is used
# to avoid resolver/runtime conflicts.
pandas>=2.2.0
numpy>=1.26.0,<2.0; python_version < "3.13"
numpy>=1.26.0; python_version >= "3.13"
scipy>=1.11.0
scikit-learn>=1.4.0
imbalanced-learn>=0.12.0

# ── Deep Learning (Optional) ────────────────────────────────────────────────
# TensorFlow is optional on Python 3.13; classical models will still run.
tensorflow>=2.16.0; python_version < "3.13"

# ── NLP ───────────────────────────────────────────────────────────────────────
nltk>=3.8.1

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Jupyter Environment ───────────────────────────────────────────────────────
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.29.0
ipython>=8.14.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0
joblib>=1.3.0
setuptools>=70.0.0

# ─────────────────────────────────────────────────────────────────────────────
# QUICK INSTALL & RUN NOTES
# ─────────────────────────────────────────────────────────────────────────────
#
# 1. Full install (recommended for Python < 3.13):
#    pip install -r requirements.txt
#
# 2. NLTK data download (auto-handled by notebook, or run manually):
#    python -c "import nltk; [nltk.download(p) for p in ['stopwords','punkt','wordnet','omw-1.4']]"
#
# 3. If using Python 3.13:
#    TensorFlow is not installed, but the notebook will gracefully skip all
#    Neural Network cells. Classical models (LR, RF) will still run completely.
#
# 4. Conda alternative (for Python 3.12):
#    conda create -n cs3012e python=3.12
#    conda activate cs3012e
#    conda install pandas scikit-learn matplotlib seaborn jupyter scipy
#    pip install -r requirements.txt --no-deps
# ─────────────────────────────────────────────────────────────────────────────

